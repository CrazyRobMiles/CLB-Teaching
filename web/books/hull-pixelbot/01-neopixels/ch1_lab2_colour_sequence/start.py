import time
import robot

robot.init()

# A list of colour constants — add the remaining colours
colours = [
    robot.RED,
    robot.GREEN,
    robot.BLUE,
    # TODO: add CYAN, MAGENTA, YELLOW, WHITE, BLACK
]

# TODO: write a for loop that calls robot.colour(c) with a 0.5 second pause
# TODO: wrap the loop in while True so it repeats forever
